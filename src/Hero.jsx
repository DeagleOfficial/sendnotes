import React from "react";
import { Typography } from "@mui/material";

const Hero = () => {
    return ( 
        <div>

            <Typography 
            style={{margin: "50px", maxWidth: "90vw"}}
            variant = "h2">Welcome to SendNotes</Typography>
        </div>
    )
}

export default Hero;